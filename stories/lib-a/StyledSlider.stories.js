import React from 'react';
import {StyledSlider} from '../../packages/lib-a/src';

export default {
    title: "StyledSlider",
    component: StyledSlider
};

export const Small = () => <StyledSlider size="small" />;